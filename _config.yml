title: Minimal theme
logo: /assets/img/logo.png
description: Minimal is a theme for GitHub Pages.
show_downloads: true
google_analytics:
theme: jekyll-theme-minimal

collections:
  travel:
    output: true

defaults:
  - scope:
      path: ""
      type: "travel"
    values:
      layout: "default"
